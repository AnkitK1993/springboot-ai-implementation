server:
  servlet:
    context-path: /springboot-ai
spring:
  application:
    name: springboot-ai-implementation
  ai:
    openai:
      api-key: ${OPENAI_API_KEY}
      chat:
        options:
          model: gpt-4o
      image:
        options:
          model: dall-e-3
